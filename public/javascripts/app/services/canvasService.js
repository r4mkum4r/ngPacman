// Generated by CoffeeScript 1.7.1
var __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

angular.module('pacman.services').service('canvasService', [
  '$q', function($q) {
    var gameCanvas;
    gameCanvas = (function() {
      gameCanvas.scope = '';

      function gameCanvas() {
        this.init = __bind(this.init, this);
      }

      gameCanvas.prototype.init = function(scope) {
        var defer;
        this.scope = scope;
        this.player = {
          id: 0
        };
        defer = $q.defer();
        return this.socket = io.connect('http://localhost:3000', {
          reconnect: false
        });
      };

      gameCanvas.prototype.updatePlayer = function(y) {
        return this.socket.emit('clientMoved', y);
      };

      return gameCanvas;

    })();
    return new gameCanvas;
  }
]);
